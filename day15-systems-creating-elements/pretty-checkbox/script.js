

// const checkbox = document.querySelector('input[type="checkbox"]');
// makeIntoPrettyCheckbox(checkbox);

// initializePrettyCheckboxes();

document.querySelectorAll('input[type="checkbox"]').forEach(checkbox => {
    makeIntoPrettyCheckbox(checkbox);
})